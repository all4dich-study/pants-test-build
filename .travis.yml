stages:
    - test
    - check
    - build
jobs:
    include:
        - stage: test
          script: echo "Hello, world"
        - stage: build
          language: java
          jdk: oraclejdk8
          script: java -version && mkdir -p ${HOME}/work && pushd ${HOME}/work && wget http://mirror.navercorp.com/apache/tomcat/tomcat-8/v8.5.23/src/apache-tomcat-8.5.23-src.tar.gz && tar -xvzf apache-tomcat-8.5.23-src.tar.gz && pushd ${HOME}/work/apache-tomcat-8.5.23-src/ && ant
        -
          language: java
          jdk: oraclejdk9
          script: java -version && git clone https://github.com/lucamilanesio/github-api.git
        - stage: check
          language: java
          jdk: oraclejdk9
          script: java -version
